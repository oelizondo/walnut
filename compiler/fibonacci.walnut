program fibonacci;

func fibonacci(int fibo) : int {
  if(fibo <= 2){
    fibo = 1;
  }
  else{
    fibo = fibonacci(fibo-2) + fibonacci(fibo-1);
  }
  puts(fibo);
  return fibo;
}

run
  fibonacci(3);
end
